[package]
name = "nexus-stellar"
version = "0.1.0"
edition = "2024"

[dependencies]
soroban-sdk = "25.1.1"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
[lib]
crate-type = ["cdylib"]
